{:paths ["src" "resources"]
 :deps {;; pin clojure here because ring-core pins version 1.9.0 which
        ;; triggers vulnerability scan.
        ;; NB app can still override by pinning
        org.clojure/clojure         {:mvn/version "1.12.0"}
        ring/ring-core              {:mvn/version "1.14.2"}}

 :aliases {:test
           {:extra-paths ["test" "test-resources"]
            :extra-deps  {io.github.cognitect-labs/test-runner {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
            :main-opts ["-m" "cognitect.test-runner"]
            :exec-fn     cognitect.test-runner.api/test}

           :coverage
           {:extra-paths ["test"]
            :extra-deps {cloverage/cloverage {:mvn/version "1.2.4"}}
            :exec-fn cloverage.coverage/run-project
            :exec-args {:src-ns-path ["src"]
                        :test-ns-path ["test"]}}
           :vuln
           {:extra-deps {io.github.clj-holmes/clj-watson {:git/tag "v6.1.0" :git/sha "be98e4d"}}
            :exec-fn clj-watson.entrypoint/scan
            :exec-args {:deps-edn-path "deps.edn"
                        :clj-watson-properties "clj-watson.properties"}}

           :build
           {:deps {io.github.clojure/tools.build {:mvn/version "0.10.10"}
                   slipset/deps-deploy           {:mvn/version "0.2.2"}}
            :ns-default build}

           :deploy
           {:deps {io.github.clojure/tools.build {:mvn/version "0.10.10"}
                   slipset/deps-deploy           {:mvn/version "0.2.2"}}
            :ns-default build}}}
